<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Market-Come Sano, Come Fresco Spa</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
     <script src="https://kit.fontawesome.com/744f01c261.js" crossorigin="anonymous"></script>
</head>

<body>
    <div class="m-5">
        {{> Menu }}
    </div>
   <div class="contain d-flex flex-column text-center justifycontent-center align-items-center">


<!-- Nombres productos -->
<div class="dashboard">
    {{#each productos}}
    {{> producto nombreProducto=this}}
    {{/each}}
</div>

<!--imágenes de productos-->
<h1>Bienvenido al mercado WEB, seleccione sus productos.</h1>
<div class="m-5">
    <div class="row">
        {{#each images}}
        <div class="col-md-4 mb-4">
            <div class="text-center">
                <h4>{{lookup ../alimentos this}}</h4> 
                <img src="/{{this}}" alt="{{lookup ../alimentos this}}" class="img-fluid">
            </div>
        </div>
        {{/each}}
    </div>
</div>

 <!-- Contenedor para el modal -->
    <div id="myModal" class="modal" tabindex="-1">
        <span class="close">&times;</span>
        <img class="modal-content" id="img01">
        <div id="caption"></div>
    </div>
        <!-- Agregar JavaScript para manejar el modal -->
        <script>
            // Obtener el modal

    // Obtener el modal
    var modal = document.getElementById("myModal");

    // Obtener la imagen y el texto de la imagen y mostrar en el modal
    var img = document.getElementsByClassName("img-fluid");
    var modalImg = document.getElementById("img01");
    var captionText = document.getElementById("caption");

    for (var i = 0; i < img.length; i++) {
        img[i].addEventListener("click", function() {
            // Mostrar la imagen seleccionada en el modal
            modalImg.src = this.src;
            modalImg.alt = this.alt;

            var productName = this.alt;

            // Actualizar el texto del pie de foto del modal
            captionText.innerHTML ="<p style='color: white;'>" + productName + "</p>";
            
            // Mostrar el modal
            modal.style.display = "block";
        });
    }

    // Obtener el botón de cierre y cerrar el modal al hacer clic en él
    var span = document.getElementsByClassName("close")[0];

    span.onclick = function () {
        // Limpiar el contenido del modal al cerrarlo
        modalImg.src = "";
        captionText.innerHTML = "";
        modal.style.display = "none";
    }
</script>
 <style>
        /* Estilos para el modal */
        .modal {
            display: none;
            /* Ocultar el modal por defecto */
            position: fixed;
            z-index: 1;
            padding-top: 90px;
            left: 0;
            top: 0;
            width: 50%;
            height: 50%;
            overflow: auto;
            background-color: rgba(0, 0, 0, 0.9);
        }

        .modal-content {
            margin: auto;
            display: block;
            width: 15%;
            max-width: 300px;
        }

        /* Estilo para el botón de cerrar */
        .close {
            position: absolute;
            top: 15px;
            right: 35px;
            color: #fff;
            font-size: 40px;
            font-weight: bold;
            transition: 0.3s;
        }

        .close:hover,
        .close:focus {
            color: #bbb;
            cursor: pointer;
        }
    </style>
<style>
        * {
            margin: 0;
        }
    
        .contain {
            background: hsl(0, 0%, 99%);
            height: 100vh;
        }
    </style>
</body>

</html>